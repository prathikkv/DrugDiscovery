---
phase: 07-ui-integration
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/pages/scorecard.py
  - src/pages/audit.py
  - src/pages/projects.py
autonomous: true

must_haves:
  truths:
    - "Scorecard page displays composite score, verdict badge, and radar chart from ScorecardResult"
    - "Scorecard page shows 7-dimension breakdown with sub-scores in expandable sections"
    - "Scorecard page offers HTML and PDF dossier generation and download"
    - "Audit trail page displays filterable records from AuditTrail.get_records()"
    - "Audit trail page can run hash chain verification and display integrity status"
    - "Projects page includes mode toggle (exploration/compliance) and showcase scenario selector with 6 scenarios in a card grid"
    - "Loading a showcase scenario creates a project and pre-populates all workflow data in session_state"
  artifacts:
    - path: "src/pages/scorecard.py"
      provides: "Scorecard page with radar chart, dimension breakdown, and dossier export"
      contains: "build_single_radar"
      min_lines: 80
    - path: "src/pages/audit.py"
      provides: "Audit trail viewer with filtering and hash chain verification"
      contains: "verify_chain"
      min_lines: 50
    - path: "src/pages/projects.py"
      provides: "Enhanced projects page with mode toggle and showcase scenarios"
      contains: "SHOWCASE_SCENARIOS"
      min_lines: 80
  key_links:
    - from: "src/pages/scorecard.py"
      to: "src/scoring/__init__.py"
      via: "ScoringFramework.score_target() and build_single_radar()"
      pattern: "ScoringFramework|build_single_radar|score_target"
    - from: "src/pages/scorecard.py"
      to: "src/reporting/__init__.py"
      via: "HTMLDossierRenderer and PDFDossierRenderer for export"
      pattern: "HTMLDossierRenderer|PDFDossierRenderer|generate_dossier"
    - from: "src/pages/audit.py"
      to: "src/compliance/audit_trail.py"
      via: "AuditTrail.get_records() and verify_chain()"
      pattern: "get_records|verify_chain"
    - from: "src/pages/projects.py"
      to: "src/pages/components/showcase.py"
      via: "SHOWCASE_SCENARIOS and load_scenario()"
      pattern: "SHOWCASE_SCENARIOS|load_scenario"
---

<objective>
Build the two results pages (Scorecard, Audit Trail) and enhance the Projects page with mode toggle and showcase scenario selector. These complete the 7-page workflow.

Purpose: The scorecard page is the culmination of the assessment -- showing the GO/CONDITIONAL/NO-GO verdict with full dimension breakdown and dossier export. The audit trail page provides regulatory compliance visibility. The enhanced projects page enables showcase demos and mode selection.

Output: Three complete Streamlit pages (scorecard new, audit new, projects enhanced).
</objective>

<execution_context>
@/Users/macbookpro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-integration/07-RESEARCH.md
@.planning/phases/07-ui-integration/07-01-SUMMARY.md

@src/app.py
@src/pages/projects.py
@src/pages/components/styles.py
@src/pages/components/hitl_gate.py
@src/pages/components/showcase.py
@src/scoring/__init__.py
@src/scoring/framework.py
@src/scoring/models.py
@src/scoring/comparative.py
@src/reporting/__init__.py
@src/reporting/data_collector.py
@src/reporting/html_renderer.py
@src/reporting/pdf_renderer.py
@src/compliance/audit_trail.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scorecard page with radar chart, dimension breakdown, and dossier export</name>
  <files>src/pages/scorecard.py</files>
  <action>
Replace the placeholder `src/pages/scorecard.py` with a full scorecard page.

**Auth + project + upstream guard**:
- Auth check + active project check (standard pattern)
- Check reasoning confidence gate in compliance mode: if `st.session_state.get(f"hitl_{pid}_reasoning_confidence_approved")` is not True, warn and st.stop()

**Scorecard computation or loading**:
1. Scorecard data key: `f"project_{pid}_scorecard"` in session_state
2. If no scorecard data exists but evidence data exists:
   - Auto-compute: Import ScoringFramework from src.scoring, reconstruct or use evidence data
   - Call `score_target(evidence, reasoning_results=reasoning_data)` -- this is fast (no API calls), just computation
   - Serialize ScorecardResult via `model_dump()` and store in session_state
   - Alternatively, if evidence is a serialized dict, reconstruct enough to call framework or show an error
3. If showcase mode, scorecard data is already in session_state from load_scenario

**Page layout**:
1. `st.title("Target Scorecard")`

2. **Verdict header**: Use verdict_badge from styles to show gene_symbol and verdict level prominently.
   ```python
   st.markdown(f"## {gene_symbol} {verdict_badge(verdict_level)}", unsafe_allow_html=True)
   ```

3. **Composite score metrics row**: 3-column layout with metric_card HTML:
   - Composite Score: `f"{score:.1f}/100"`
   - Verdict: level string
   - Data Coverage: average coverage across dimensions

4. **Radar chart**: Import `build_single_radar` from `src.scoring`. Reconstruct ScorecardResult from the scorecard dict using `ScorecardResult(**scorecard_data)` (or `ScorecardResult.model_validate(scorecard_data)` for Pydantic v2). Call `build_single_radar(scorecard_result)` and display with `st.plotly_chart(fig, width="stretch")`.

5. **Dimension breakdown**: For each of the 7 dimensions in `scorecard.composite.dimension_scores`:
   - Expander with dimension name (title-cased, underscores to spaces) and score fraction
   - Inside: st.progress bar showing score/max_score ratio
   - Data coverage caption
   - Sub-scores list: for each sub in `dim.sub_scores`, show name, value/max_value, description
   - If forced_conditional flag is set and this dimension is in violations, show warning_panel

6. **Forced conditional warning**: If `verdict.forced_conditional` is True, show alert_panel explaining which dimensions triggered the downgrade.

7. **Export section**: `st.divider()`, `st.subheader("Export Assessment Dossier")`
   - Two columns:
   - Column 1: "Generate HTML Dossier" primary button
     - On click: Import `collect_dossier_data` from src.reporting, `HTMLDossierRenderer`
     - Build DossierData from scorecard, evidence, reasoning data in session_state
     - Render HTML string
     - Offer download: `st.download_button("Download HTML", data=html_str, file_name=f"{gene_symbol}_dossier.html", mime="text/html", key="scorecard_download_html")`
   - Column 2: "Generate PDF Dossier" secondary button
     - On click: Import `PDFDossierRenderer`
     - Render PDF bytes
     - Offer download: `st.download_button("Download PDF", data=pdf_bytes, file_name=f"{gene_symbol}_dossier.pdf", mime="application/pdf", key="scorecard_download_pdf")`
   - Wrap generation in try/except and show st.error on failure

8. **Navigation**: `st.page_link("src/pages/audit.py", label="View Audit Trail", icon=":material/arrow_forward:")`

All widget keys prefixed with `scorecard_`.

Handle the case where scorecard_data is a dict (from JSON) vs a Pydantic model gracefully. Extract fields with `.get()` for dicts and attribute access for models. Use `isinstance` check or try `model_validate` first.
  </action>
  <verify>
Run `python -c "import src.pages.scorecard; print('scorecard page OK')"` to confirm imports succeed. Run `python -c "from src.scoring import build_single_radar, ScorecardResult; print('scoring imports OK')"` to verify scoring API is accessible.
  </verify>
  <done>
Scorecard page reconstructs or loads ScorecardResult, displays verdict badge with composite score, renders radar chart via build_single_radar, shows 7-dimension breakdown with sub-scores, and offers HTML/PDF dossier download. Upstream gate check enforced in compliance mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit trail page and enhanced projects page with showcase scenarios</name>
  <files>
    src/pages/audit.py
    src/pages/projects.py
  </files>
  <action>
**`src/pages/audit.py`** -- Audit Trail Viewer:

**Auth guard**: Standard auth check at top.

**Page layout**:
1. `st.title("Audit Trail")`
2. Description caption: "Complete audit trail with hash chain integrity verification for 21 CFR Part 11 compliance."

3. **Hash chain verification section**:
   - "Verify Chain Integrity" button (key="audit_verify_chain")
   - On click: Create `AuditTrail()`, call `verify_chain()`
   - If result["valid"]: `st.success(f"Chain integrity verified. {result['records_checked']} records checked.")`
   - If not valid: `st.error(f"Chain broken at record {result['first_broken']}: {result['error']}")`
   - Use insight_panel from styles for the summary

4. **Filters**:
   - Sidebar or columns with:
     - `st.selectbox("Resource Type", options=["All", "project", "hitl_gate", "task", "reasoning"], key="audit_filter_resource_type")`
     - `st.number_input("Max Records", value=100, min_value=10, max_value=1000, key="audit_filter_limit")`

5. **Records display**:
   - Create `AuditTrail()`, call `get_records(resource_type=selected or None, limit=limit)`
   - Display as st.dataframe with columns: sequence_id, timestamp, user_id, action, resource_type, resource_id
   - Each record expandable to show full details_json
   - Alternative: use st.table for small datasets or st.dataframe for larger ones
   - Show record count as metric

6. **Record detail view**: When user clicks/expands a record, show:
   - Full details_json in st.json()
   - Record hash and previous hash in st.code()
   - Signature info if action is "SIGN"

All widget keys prefixed with `audit_`.

---

**`src/pages/projects.py`** -- Enhanced Projects page:

Rewrite the existing projects page (read it first!) to add mode toggle and showcase scenarios while preserving all existing functionality (project CRUD, delete, list).

**Preserve**:
- Auth guard at top (existing pattern)
- ProjectService instantiation
- Create project form (enhanced with mode toggle)
- Project list with delete buttons

**Add/Modify**:

1. **Create project form enhancement**:
   - Add `st.radio("Analysis Mode", options=["exploration", "compliance"])` inside the form, with format_func that shows:
     - "Exploration -- HITL gates auto-approve for rapid iteration"
     - "Compliance -- HITL gates require electronic signature (21 CFR Part 11)"
   - Pass `project_config={"mode": mode}` to `ps.create()`
   - After creation, set `st.session_state["active_project_id"] = project.project_id` and `st.session_state["project_config"] = {"mode": mode}`

2. **Showcase Scenarios section** (after create form):
   - `st.divider()`
   - `st.subheader("Showcase Scenarios")`
   - `st.caption("Pre-loaded pharma scenarios with cached evidence. No live API calls required.")`
   - Display 6 scenarios in a 3-column grid (2 rows of 3)
   - For each scenario in SHOWCASE_SCENARIOS:
     - `st.container(border=True)` with:
       - `st.markdown(f"**{name}**")` -- scenario name
       - `st.caption(scenario["description"])` -- description with drug/company context
       - `st.caption(f"Company: {scenario.get('company', 'N/A')}")` -- pharma company
       - "Load Scenario" button with key `f"projects_load_{name}"`
   - On load: call `load_scenario(name)` from showcase module, create project via `ps.create()` with name=f"Showcase: {name}", project_config={"mode": "exploration", "showcase": name}. Pre-populate ALL workflow data in session_state:
     - `st.session_state[f"project_{pid}_evidence"] = data.get("evidence")`
     - `st.session_state[f"project_{pid}_reasoning"] = data.get("reasoning")`
     - `st.session_state[f"project_{pid}_scorecard"] = data.get("scoring")`
     - `st.session_state[f"project_{pid}_pipeline_result"] = data.get("pipeline_report")`
     - `st.session_state["active_project_id"] = pid`
     - `st.session_state["project_config"] = {"mode": "exploration", "showcase": name}`
   - st.rerun() to refresh page

3. **Project list enhancement**:
   - When user clicks on a project (not delete), set it as active:
     - Add "Open" button alongside "Delete" button
     - On open: `st.session_state["active_project_id"] = project.project_id`
     - Load project config from project.config_json: `json.loads(project.config_json)` and store in `st.session_state["project_config"]`
   - Show active project indicator (highlight or badge) if `project.project_id == st.session_state.get("active_project_id")`

All widget keys prefixed with `projects_`.

Import `SHOWCASE_SCENARIOS, load_scenario` from `src.pages.components.showcase` at top.
  </action>
  <verify>
Run `python -c "import src.pages.audit; print('audit page OK')"` and `python -c "import src.pages.projects; print('projects page OK')"` to confirm imports succeed. Run `python -m pytest tests/ -x -q --timeout=30` to verify no regressions.
  </verify>
  <done>
Scorecard page shows verdict, radar chart, dimension breakdown, and dossier export. Audit trail page displays filterable records with hash chain verification. Projects page includes mode toggle, showcase scenario selector with 6 scenarios, and project activation. All 7 pages of the application are complete. No test regressions.
  </done>
</task>

</tasks>

<verification>
1. `python -c "import src.pages.scorecard; print('OK')"` -- scorecard page imports cleanly
2. `python -c "import src.pages.audit; print('OK')"` -- audit page imports cleanly
3. `python -c "import src.pages.projects; print('OK')"` -- enhanced projects page imports cleanly
4. `python -m pytest tests/ -x -q --timeout=30` -- all existing tests pass
5. All 7 pages importable: `python -c "import src.pages.login, src.pages.projects, src.pages.omics, src.pages.evidence, src.pages.insights, src.pages.scorecard, src.pages.audit; print('All 7 pages OK')"`
</verification>

<success_criteria>
- Scorecard page: shows verdict badge, composite score metric, radar chart, 7-dimension breakdown with sub-scores, and HTML/PDF dossier download
- Audit trail page: displays filterable audit records and performs hash chain integrity verification
- Projects page: mode toggle (exploration/compliance), 6 showcase scenarios in card grid, project activation, all existing CRUD preserved
- All 7 pages import without errors
- All 171 existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-integration/07-03-SUMMARY.md`
</output>
